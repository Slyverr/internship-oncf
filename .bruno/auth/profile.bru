meta {
  name: /profile
  type: http
  seq: 2
  tags: [
    Auth
  ]
}

get {
  url: {{baseUrl}}/auth/profile
  body: none
  auth: inherit
}

vars:post-response {
  user_id: res.body.id
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/auth/profile
    method: GET
    mode: none
  }
  
  response: {
    headers: {
      Content-Type: application/json
    }
  
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: json
      content: '''
        {}
      '''
    }
  }
}
