meta {
  name: get
  type: http
  seq: 3
  tags: [
    Orders
  ]
}

get {
  url: {{baseUrl}}/orders/:id
  body: none
  auth: inherit
}

params:path {
  id: {{order_id}}
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/orders/:id
    method: GET
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
