meta {
  name: get
  type: http
  seq: 2
  tags: [
    Users
  ]
}

get {
  url: {{baseUrl}}/users/:id
  body: none
  auth: inherit
}

params:path {
  id: {{user_id}}
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/users/:id
    method: GET
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
