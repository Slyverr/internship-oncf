meta {
  name: delete-order
  type: http
  seq: 3
  tags: [
    Cleanup
  ]
}

delete {
  url: {{baseUrl}}/orders/:id
  body: none
  auth: inherit
}

params:path {
  id: {{order_id}}
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/orders/:id
    method: DELETE
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
