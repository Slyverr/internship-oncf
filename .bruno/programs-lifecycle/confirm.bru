meta {
  name: confirm
  type: http
  seq: 5
  tags: [
    Programs
  ]
}

post {
  url: {{baseUrl}}/programs/:id/confirm
  body: none
  auth: inherit
}

params:path {
  id: {{program_id}}
}

example {
  name: 201 Response
  
  request: {
    url: {{baseUrl}}/programs/:id/reject
    method: POST
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
