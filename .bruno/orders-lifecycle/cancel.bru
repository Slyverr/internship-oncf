meta {
  name: cancel
  type: http
  seq: 7
  tags: [
    Orders
  ]
}

post {
  url: {{baseUrl}}/orders/:id/cancel
  body: none
  auth: inherit
}

params:path {
  id: {{order_id}}
}

example {
  name: 201 Response
  
  request: {
    url: {{baseUrl}}/orders/:id/cancel
    method: POST
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    headers: {
      Content-Type: application/json
    }
  
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: json
      content: '''
        {}
      '''
    }
  }
}
