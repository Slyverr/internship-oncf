meta {
  name: reject
  type: http
  seq: 5
  tags: [
    Orders
  ]
}

post {
  url: {{baseUrl}}/orders/:id/reject
  body: json
  auth: inherit
}

params:path {
  id: {{order_id}}
}

body:json {
  {
    "reason": "Rejection reason for order #{{order_id}}"
  }
}

example {
  name: 201 Response
  
  request: {
    url: {{baseUrl}}/orders/:id/reject
    method: POST
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    headers: {
      Content-Type: application/json
    }
  
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: json
      content: '''
        {}
      '''
    }
  }
}
